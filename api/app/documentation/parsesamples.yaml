consumes:
  - multipart/form-data
parameters:
  - description: sample file to upload
    in: formData
    name: samples
    required: true
    type: file
  - description: file delimiter
    in: formData
    name: delimiter
    required: true
    type: string
produces:
  - application/json
responses:
  200:
    description: The current request is ongoing
    schema:
      properties:
        data:
          properties:
            invalid:
              type: integer
            nc:
              type: integer
            samples:
              items:
                properties:
                  genes:
                    items:
                      type: number
                    type: array
                  sampleID:
                    type: string
                  typeid:
                    type: string
                type: object
              type: array
          type: object
  status:
    type: string
  400:
    description: Task failed due to an issue with client input
  500:
    description: An interval server error has occured
summary: Parse CSV file for 440 genes and types
tags:
  - Parse Samples
